# Python
__pycache__/
*.py[cod]
*.so
*.egg-info/
dist/
build/
*.egg

# 本地数据（数据库不纳入git，由GitHub Actions缓存持久化）
# 历史数据来自仓库中的 seed_data.json（纯本地模式，无需体彩API）
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm

# 日志
logs/*.log
!logs/.gitkeep

# IDE
.vscode/
.idea/
*.swp

# 环境变量
.env
config/local.yaml

# 临时文件
submission_result.json
push_result.json
predict_diagnostic.json

# WorkBuddy 本地项目数据
.workbuddy/

# 本地运行日志（JSON 日志不纳入仓库）
logs/*.json
